= form_for :session, url: sessions_path do |f|
  .row
    .col.s10.offset-s1
      %h2.app-typography--header-1 Log In

      .input-field
        = f.text_field :email
        = f.label :email do
          Email

      .input-field
        = f.password_field :password
        = f.label :password do
          Password

      = render_component 'button', submit: f do
        Login
